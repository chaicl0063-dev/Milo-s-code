/**
 * 官网配色，唯一来源（DESIGN.md「Colors」）。2026-09-15 用户定「浅色底、科技感」：
 * 底色从暖奶油改为中性近白，卡片白底细边，文字冷一点；强调色仍是陶土橙，因为应用界面本身就是这套颜色，
 * 首屏放的是真实截图，官网如果换成蓝色，产品在自己的首页里会像外来物。要换强调色只改这里。
 */
export const T = {
  /** 页面底色：中性近白，不是纯白，不带蓝 */
  bg: "#F7F7F5",
  /** 交替段落 / 卡片的浅面 */
  paper: "#EFEFEC",
  /** 卡片、输入框内的白 */
  white: "#FFFFFF",
  ink: "#17181A",
  muted: "#66696E",
  line: "#E3E3DF",
  /** 唯一强调色（按钮、选中、播放中） */
  accent: "#D9633A",
  /** 深一档：小链接、小标签文字 */
  deep: "#A84427",
  /** 仅数字/小标注 */
  gold: "#B8811C",
  /** Milo 的颜色，只在人物旁边出现 */
  teal: "#2F5D62",
  /** 唯一的深色段落 */
  dark: "#17181A",
  onDark: "#F7F7F5",
} as const;

export type SiteTheme = typeof T;
