# ReAround You · Claude × Codex 协作文档

更新：2026-09-14
状态：待 Claude 读取并核对代码；本文件建立不代表任务已派发、功能已修改或图片已生成。
产品依据：同目录 PRODUCT-BRIEF.md（2026-09-14）；用户已认可本轮产品评审，并要求共同推进项目。

## 1. 用户授权与本轮范围

用户要求：由 Claude 与 Codex 协作完成项目；把已认可的评审意见写入本文件供 Claude 修改；项目所需新增生成图片由 Codex 负责；官网增加更好的交互体验。

- 保留产品方向：海外市场、英文默认、全球可探索而非指定首发城市；三项导航 Around / Guide / Me；详情页与讲解对话页分离；Mia / Milo；个人订阅方向；PWA 为主。
- PRODUCT-BRIEF.md 的设计部分不是定稿。配色、字体、图片形式、布局、动画均可调整，不应因其写着“已定”而阻止合理改进。
- 本轮重点是产品能力状态、核心使用流程、数据质量、官网演示与应用一致性。
- 不推翻已经实现的路线、地图联动、地点讲解、音频与拍照能力。先核对、修补、复用。
- 不扩大成 OTA、攻略社区、创作者平台或通用 AI 聊天产品。

## 2. 协作角色与文件边界

| 角色 | 主要负责 | 交付形式 |
|---|---|---|
| Milo（用户） | 产品取舍、阶段体验反馈 | 在本文件或对话中给出意见 |
| Claude | 核对代码与开放状态；数据筛选；应用流程；官网交互实现；性能与测试；接入最终图片 | 代码、验收结果、本文件状态更新 |
| Codex | 产品与体验评审；新增图片生成及适配；官网交互方案与视觉复核 | 本文件、图片及清单、截图评审 |

协作约定：
1. 本文件是本轮任务、交接与状态的共同入口；PRODUCT-BRIEF.md 保留为产品事实说明。
2. Claude 核实事实后更新 PRODUCT-BRIEF.md，区分实现状态、开放状态和商业状态；不要把建议写成已完成。
3. 双方开始工作前读本文件与项目 AGENTS.md。写代码时遵循项目当前 Next.js 文档要求。
4. 使用任务编号更新状态：待核对 / 待实施 / 进行中 / 待验收 / 已完成 / 阻塞。完成须附证据。
5. 不同时修改同一源码文件。需 Codex 直接改代码时，先登记涉及文件与负责人。
6. 新增 AI 图片由 Codex 生成。Claude 不自行生成或替换为不相关图库图；可复用现有合适图片，并注明待替换位置。
7. 修改可在本地完成；发布按用户已有授权执行。本文件本身不构成新的生产发布指令。
8. 文件是异步交接入口，不假定任一助手会自动收到或持续监听更新。每次交接写清变更和下一步。

## 3. 共同产品主线

核心体验：用户已经在陌生街区，看见一个地方，点一下，导游开始解释，并允许继续追问。

能力层次：
- 第一层“看懂这里”：地图选点、拍照识别、讲解、追问。
- 第二层“继续探索”：附近值得看的地方、短时路线、下一站。
- 第三层“旅行便利与记录”：翻译、收藏、离线内容、旅行日记。

官网优先说服用户体验第一层；应用顺势引出第二层。第三层在需要时出现，不在首次使用时挤占核心入口。
独自旅行是核心画像，两三人同行也适用；对外可用“按自己的节奏探索”，无需把独行写成使用门槛。

## 4. 已知事实与待核对项

### 4.1 文档已说明，勿重复开发

- 地图地点卡已有“听导游讲讲”和“查看”。
- 地点详情与讲解对话独立；讲解页带地点上下文，自动朗读、逐句显示，底部固定追问。
- 已处理手机浏览器限制自动出声时的手动播放提示。
- 已有按时间与兴趣生成路线、地图编号图钉、路线栏、当前站卡片。
- 已有两位导游、八种讲解语言、收藏和离线下载等实现描述。

这些来自产品文档，不等于 Codex 已逐项验证代码和真机行为。

### 4.2 上次线上观察，须在当前版本复核

- 河内附近列表把 North Vietnam、Siege of Songping、International Control Commission 等条目与实体地点混排，多个条目显示相同很近的距离。
- 官网 Plan my day 进入应用后先选导游，随后落到河内地图，未延续演示场景。
- 还剑湖详情长文之后才有讲解入口；但地图卡另有快捷入口，不可概括为所有入口都被埋住。
- 地点讲解页曾加载失败一次，重载后恢复。记录为需复查问题，不认定整个功能不可用。
- 生成讲解出现没有展示出处的第一人称回忆式小故事；需要核对提示词和来源策略。
- 官网英文内容与浏览器自动翻译应区别评审，不把自动翻译的中文错误归因于应用原文。

## 5. Claude 开发任务

### C01 · 功能状态总账（优先 P0，待核对）

逐项核对：地点讲解、随手问、拍照识别、翻译、路线、旅行者观点、主题漫步、日记、离线、账号。
每项记录：实现状态 / 用户开放状态 / 商业状态 / 入口 / 验证方式。
重点解决 PRODUCT-BRIEF 3.3“路线为未开放 Plus”与 4.4“已实现路线”的口径。
实现了、开放测试、已对公众开放、未来收费不是同一个状态。无用户数据库或只有邮箱登记时，不得把邮箱登记描述为完整账号系统。
验收：官网、应用与产品文档对同一能力的描述一致。

### C02 · 可到访地点筛选（P0，待实施）

把实体地点与百科背景资料分开：建筑、广场、公园、寺庙、店铺等可进入附近推荐；历史政权、事件、人物、机构历史用于讲解背景；行政区与城市用于区域介绍。
筛选逻辑基于实体类型、地点性质及可用证据，避免只按名称关键词误删真实遗址。无法判断的条目退出默认推荐，保留资料检索价值。
同步应用于地图、列表、路线候选；处理重复条目和粗略坐标带来的误导距离。
无真实定位时明确显示示例位置或当前浏览区域，不让用户误以为已定位到本人。
验收：已知异常条目不再作为独立可到访站点出现；实体遗址仍可正常出现；路线候选同样干净。

### C03 · 最短讲解路径（P0，待实施）

保留地点卡直达讲解。详情页顺序优先：地点图片与名称 → 推荐/介绍短句 → 听讲解与追问入口 → 到这里/地图入口 → 可展开详情与来源。
坐标保留在详细信息中，首要信息用地址、距离等用户能理解的内容；无可靠步行计算时不编造时间。
讲解的等待、准备音频、手动开始播放、失败重试必须有可理解状态；重试不丢地点。
首次引导尽量轻；官网带入的导游、语言和示例上下文不要被无故重置。
“三秒”先作为内部目标：测量点击听讲解到首句有信息量的音频，区分缓存/首次生成/失败重试；未验证前不作无条件官网保证。
验收：用户不必读完长文即可开讲；静音/暂停与追问可发现；错误可恢复。

### C04 · Guide 页按场景组织（P1，待实施）

保留 Around / Guide / Me。Guide 以当前探索为中心，而不是未上线功能目录。
主要场景：What’s this place? / What’s worth seeing nearby? / I have an hour.
拍照翻译保留明确工具入口；餐饮待数据就绪再加入；未上线预告降低层级。
从地点进入时直接解释该地点；从 Around 进入时继承浏览区域/选中地点；没有上下文才让用户选位置或示例；有路线时优先继续路线。
一小时场景：有位置则使用已有时间默认值、按需确认兴趣；无位置先选区域。不要重复询问已知信息。
按钮以可理解为先：营销可以写情境，操作可以使用准确功能名。
验收：每个场景都到达明确结果；不能使用的能力在点击前可识别。

### C05 · 路线的可执行性（P1，待核对及修补）

复用已有 2—7 站生成与地图联动。优先做好“一小时附近步行”，再扩展半天/一天。
- 步行模式默认不插入乘车；需交通时明确让用户选择；不为凑站数牺牲地点质量。
- 总时长应包含行走、停留和合理缓冲。
- 核对距离是直线还是道路距离；入口、绕路、跨河等影响可到达性。
- 虚线若仅连接站点，应标为顺序示意，不能冒充真实导航路径。
- 地图、路线栏、地点卡与讲解共享站点名称、编号、当前选择。
- 返回地图保留缩放、选中地点和路线；支持跳过站点后保持状态一致。
验收：至少提供一条实际可复核的路线记录，说明时长、交通方式、站点与地图关系；记录无法验证部分。

### C06 · 导游事实与现场表达（P1，待实施）

区别事实、传说、建议；不让 Mia/Milo 用无依据的“我记得”“我常来”伪造亲历。
没有可靠朝向时，不直接说“你左手边”；可改为“面对正门时左侧”或先确认位置。
人格体现在语气、节奏、重点，不在虚构身份。
讲解先短后长，给相关追问；来源可访问。避免为固定字数或故事性增加没有依据的细节。
验收：选取若干地点检查生成结果与出处；低信息场景不强行编故事。

### C07 · 餐饮与人群承诺（P2，待实施）

先接入并验证实体咖啡馆/餐馆，再实现按条件选择；“当地人常去”“更好吃”需要额外依据。
缺少营业与客流数据时不声称现在营业、无需排队、一定安静。
官网当前超前的餐饮/避开人群演示需改为真实可兑现的表达，或明确标注未来概念。
验收：存在地点、符合条件、推荐依据分别清楚；不将 OSM 类别本身当作质量评价。

### C08 · 未开放功能与商业表达（P1，待实施）

建议修改原“Paid feature”：未开放时用 Coming soon，辅以 Planned for Plus；已有可购买能力才用付费标签。
允许点击预览并由导游简短解释，但不要让几张主要卡都通往“还不能用”。
Free 随手问与 Plus 路线的区别优先体现为结构化、可保存、可调整、地图连续执行的体验，而非禁止用户自然地问行程。
不在本轮制定价格；“永久免费”等承诺与实际计划核对。
验收：用户在点击前知道当前能否使用，不误以为付钱即可解锁未完成能力。

## 6. 官网交互改版（Claude 实现，Codex 评审与图片）

### W01 · 核心演示：同一地点，从看见到听懂（P1）

首屏情境按钮保留页内演示，不强制跳应用。第一版优先展示现有可用能力。
推荐演示选一个真实地点贯穿：地点照片 → 地图选点 → 导游简短讲解 → 一个追问 → 继续探索。
明确“Interactive demo / Sample conversation”；预写内容不可伪装为实时 AI 响应。
场景切换同步更改照片、地点名、地图选中点、讲解及入口上下文，而不只换文字气泡。
默认静态内容可完整阅读；用户操作后才播放声音；提供重播、暂停与停止。不以自动长篇打字阻挡阅读。
地图、路线与聊天 UI 应复用真实产品组件或数据结构，不让生图生成伪截图来充当交互。
验收：鼠标、键盘、触屏均可切换；示例可完整读完；未加载音频时文字仍可用。

### W02 · Mia / Milo 试听比较（P1）

使用同一地点、同一主题的短片段试听，便于比较讲述风格；显示时长、播放状态和停止入口。
两段音频互斥播放。选择“和 Mia/Milo 开始”时将选择带入应用。
头像不等同于真人导游资质；生成的人物形象应以 AI 导游角色呈现。
验收：实际声音与应用一致；不依赖悬停；切换不会叠音。

### W03 · 短路线预览（P1，依赖 C01/C05）

时间/兴趣改变后，真实示例的站点顺序、地图标记和时长摘要同步更新；点击站点高亮地图并显示地点卡，支持还原。
尚未开放时明确“路线功能预览”，主入口指向预览或上线通知；不要使用会让人以为立即可规划的按钮。
示例所用路线需核验；预计算示例可优先于每次调用 AI，保证演示稳定，但必须如实标为示例。
验收：地图与列表对应；当前站清楚；任何路径都有返回/重置；不把装饰虚线当导航。

### W04 · 官网到应用的连续入口（P1）

主入口围绕“探索附近”；次入口为无需真实定位的示例体验。
传递明确的场景、地点、导游、语言；在应用侧校验并设置合理回退。不得在分享链接中夹带用户精确位置或私人会话。
位置只在执行相关任务时请求；拒绝后能选区域或继续示例。
Android 未开放时清楚写即将推出；不把下载作为 Web 体验前置条件。
验收：官网选择 Mia + 某地点后，应用不会无说明重置到另一位导游或无关地点。

### W05 · 页面互动原则与响应式（P1）

- 互动必须帮助用户理解产品，不增加装饰性任务。
- 不做滚动劫持、强制横向滚动、鼠标跟随特效、全屏自动视频或自动语音。
- 支持减少动态效果设置；动画关闭后信息与操作完整。
- 手机主动作紧跟价值说明，演示采用紧凑布局，不堆一串长胶囊和巨大手机空壳。
- 桌面可用地图与讲解并排，手机分层切换；共享同一状态，不要求机械地把手机纵向页面横排。
- 图片按宽度提供合适版本，非首屏内容按需加载；图片和演示预留空间，避免加载后跳动。
- 明确选中、加载、无结果、失败、重试和演示结束状态；不让颜色成为唯一提示。
- 第一版不增加城市货架；全球覆盖与一个城市演示可同时成立。

推荐页面顺序：价值与互动示例 → 真实地点完整体验 → 当前可用的少量场景 → 导游试听 → 来源与使用条件 → 开始体验及明确标注的未来能力。

## 7. 图片全部交给 Codex：需求与交付规则

### 7.1 原则

先按页面槽位定义题材、裁切、比例与留白，再生成图片；不先批量生成好看但不适合页面的图。
品牌氛围图可以生成；真实地点识别、地点详情证据、真实导航地图不能用想象图替代。真实地标使用生成图时不得将其当作现场事实依据。
真实功能界面采用应用组件/截图；地图用真实地理数据渲染；AI 图片不绘制文字、按钮、线路或数据。
生成图不是摄影师拍摄记录，图注、替代文本与素材归属中不可虚构摄影地点、人物身份或来源。

### 7.2 首批图片槽位（待页面骨架确认，尚未生成）

| 编号 | 使用位置 | 图片任务 | 暂定交付比例 | 负责人 | 状态 |
|---|---|---|---|---|---|
| IMG-01 | 官网主视觉 | 有人正在探索街区的生活现场，保留明确文字留区，主体不压标题 | 桌面横图约 16:9，移动端独立竖图约 4:5；按实际槽位修订 | Codex | 待槽位确认 |
| IMG-02 | Mia 角色 | 与产品语气一致的 AI 导游形象，便于小尺寸辨识 | 1:1，另交适配裁切 | Codex | 待角色视觉样张 |
| IMG-03 | Milo 角色 | 与 Mia 同系列，但性格与识别度不同 | 1:1，另交适配裁切 | Codex | 待角色视觉样张 |
| IMG-04 | 当前能力场景配图 | 行中观察、听讲解、继续探索；餐饮未开放时不把餐饮放主位 | 按组件最终比例 | Codex | 待页面需求 |
| IMG-05 | 补充情绪段（如保留） | 只在页面确有叙事作用时生成，不为填满长页增加图片 | 按需 | Codex | 可选，未启动 |

Claude 在下面补充每个实际槽位：组件/文件路径、桌面与手机尺寸、最窄裁切、文字覆盖区域、主体位置、背景明暗、页面截图。比例暂定，可按设计改变，不需为了表格固定比例牺牲页面。

### 7.3 素材交付

项目内建议位置：原图与生成说明放 assets/generated/；网页优化资源放 public/images/；图片清单放 docs/IMAGE-MANIFEST.md。
这是约定的后续位置，本次没有创建或填充这些图片目录。
Codex 交付：素材编号、预览、文件名与路径、尺寸、生成说明、用途、裁切焦点、替代文本、是否概念图、推荐网页版本。
Claude 负责组件接入与响应式配置；Codex 看桌面和手机实际页面，必要时重新生成或调整裁切。
先做一组样张（主视觉或两位导游之一组）检验方向，再扩展整站，不一次铺满未经验证的素材。

## 8. 验收与交接

本轮不要求先把所有功能做完再展示。建议分三批：
1. C01/C02/C03：状态准确、地点可信、讲解顺畅。
2. C04/C05/C06/C08 与 W01/W02/W04：核心流程和官网演示一致；搭建待配图骨架。
3. W03/W05、图片接入、餐饮后续：完善连续探索与视觉适配。

测试重点：
- 对数据过滤、路线时长/站点规则、入口上下文编写有意义的验证；静态文案或轻微样式修改不堆形式测试。
- 覆盖首次/再次进入、无定位/示例位置、地图/列表到讲解、音频受限、网络失败、返回地图不丢状态。
- 覆盖窄屏手机、常见手机、桌面；键盘、触控、减少动画；不把桌面模拟当成真机音频与定位验收。
- 记录实际执行过的检查，不把未测项写成通过。

### 交接日志

| 日期 | 操作者 | 完成事项 | 下一步 |
|---|---|---|---|
| 2026-09-14 | Codex | 读取产品需求；修正前次能力判断；建立本协作文档，整理产品任务、交互方案与图片分工 | Claude 核对 C01，复查 C02/C03；在下方登记代码状态与图片槽位 |
| 2026-09-14 | Claude | R01–R04 修补并加测试；C02 第一版（Wikipedia type/dim + 描述类型词过滤）；官网文案对齐 4.7；登记图片槽位；PRODUCT-BRIEF 4.4/4.8 修正 | Codex：按槽位表出 IMG-01 与 IMG-02/03 样张；评审 /site 与 /guide。Claude：C03 计时埋点、C02 第二版 |
| 2026-09-14 | Claude（转达用户） | 用户决定设计系统重新放开讨论；PRODUCT-BRIEF 升 v3 | Codex：对照 PRODUCT-BRIEF 第 6 节逐条给设计方案（保留 / 调整 / 理由 / 示例），图片样张可与方案一起出 |
| 2026-09-14 | Claude | 读第 10/11 节与两份提案；接入 IMG-01（`public/images/hero-*.jpg`，手机竖版裁切，页脚注明 AI 生成）；`PHOTOS.eat`→`look`；新建 `docs/IMAGE-MANIFEST.md`；C03 计时埋点（`lib/metrics.ts`，控制台 + localStorage 最近 30 条）；统一 PRODUCT-BRIEF 4.4/4.8 与代码口径 | 用户：对设计提案 v1 逐项拍板（见下方「需要用户决定」）；用户手动生成 IMG-02/03。Codex：抽查本轮变更（通过 / 需修改 / 待确认） |
| 2026-09-14 | Claude | 用户对设计提案 v1 七项全部采纳建议：首屏改方案 A、副题与按钮文案、去数字、容器圆角 20、陶土两档；接入 IMG-02～06 与手机竖版首屏图，`PersonaAvatar` 改用肖像；逐句显示替代逐字；PRODUCT-BRIEF v4 | Codex：抽查 /site 桌面与手机首屏、导游肖像在 24px 的辨识度；Claude：C02 第二版、隐私条款页 |
| 2026-09-14 | Claude | 按第 14 节修 C03 计时：首句以音频 playing / 系统语音 onstart 为准，解锁静音片段不算；一次尝试一条记录可随状态更新（blocked 后手动播放补真实时间）；每个请求独立计时器，重试不串；静音/失败/中止有终态；新增入口点击→请求启动一段；6 条测试 | Codex：抽查 useNarrator 事件点与 metrics 终态逻辑。Claude：I01 / I02 |### Claude 交接 · 2026-09-14（第一轮）
| 2026-09-14 | Claude | I01 与 I02 上线：真实地点（圣雅克塔）演示面板，看见→讲解→追问→进应用；双导游同题对比试听（互斥、时长、停止、重播、Start with X 带 ?persona= 进应用）；四情境演示移除 | Codex：按 W01/W02 验收点抽查（脚本事实、音频状态、切换停旧音频、无音频可读）。Claude：I03 或 C02 第二版，看 Codex 排序 |
- **本轮任务编号**：R01、R02、R03、R04、C02（第一版）、C05（预算部分）、C08（复核）。C03 未动。
- **已核实的实际功能/开放状态**：与 PRODUCT-BRIEF 4.7 一致；本轮未新增开放能力。半天/一天路线仍是「基础版，所有人可用，未来 Plus」。
- **与文档不符的事实及依据**：
  - Codex 4.2 观察的河内非实体条目（North Vietnam、Siege of Songping、International Control Commission、Hoàn Kiếm district、Red River Delta、Alcatraz Gang、State Bank、Stock Exchange、Archdiocese、Hospital、School）在当前版本复现，全部来自 Wikipedia 坐标搜索；「相同的很近距离」是这些词条的坐标落在同一个默认点（Wikipedia 返回的 dim=10000、type=city 等区域坐标）。
  - PRODUCT-BRIEF 4.4 原文「有百科正文」不准确，代码只检查有无百科条目；已改为「有百科条目或图片，只是相对排序」，并新增 4.8 说明实体过滤。
- **修改文件与完成内容**：
  - R01：`lib/planCore.ts`（新，从 `lib/plan.ts` 拆出纯函数）。`assemble` 每一站都按「与展示一致的含缓冲公式」校验，首站也校验；超预算的站跳过而不是终止；空路线总时长为 0。`lib/plan.ts` 改为调用它。
  - C02 第一版：`lib/places/visitable.ts`（新）+ `lib/wikipedia.ts`。Wikipedia 坐标搜索加 `gsprop=type|dim`；type 为 country/state/adm*/city/event/satellite/edu 的排除；dim 大于 20000 的排除（Wikipedia 对 city、waterbody 默认填 10000，不能当区域）；Wikidata 描述以类型词开头（国家、战役、机构、行政区、人物团体等）的排除，遇到场所词（museum、temple、street、lake…）看谁先出现。没有描述的保留。过滤只作用于「周边列表 / 地图 / 路线候选」，详情页仍可打开这些词条作背景资料。
  - R02：`components/site/SiteLanding.tsx` 试听句、路线演示对话、场景屏引语改写：去掉 skip the queue、almost nobody finds、minus the crowd、on your left；方位全部改为「facing the church, on the right」这类有前提的表达；路线演示加「Sample conversation」标注。
  - R03：Free/Plus 标题改为 "Listening and short walks are free. Plus keeps your longer days."；Free 卡副题 Forever 改为 No account needed；Free 清单列出一小时路线和「半天/一天路线（基础版，测试期）」；Plus 清单首项改为「Save, adjust and pick up your routes across days」。
  - R04：见上文文档修正；`score()` 注释明确它不是实体证明。
  - 测试：`tests/planCore.test.ts`（8 条：缓冲公式、首站拦截、一小时不乘车、6 公里上限、空路线、最近邻、跳过后续站）与 `tests/visitable.test.ts`（4 组，用河内实际条目）。运行 `pnpm test`（`node --import tsx --test`，新增 devDependency tsx）。
- **实际验证结果与剩余问题**：
  - `pnpm test` 12/12 通过；`tsc`、`eslint`、`next build` 通过。
  - 本地 `/api/nearby?lat=21.0285&lon=105.8542&radius=1000&sources=fast`：过滤前 30 条，过滤后 16 条；保留 Hoàn Kiếm Lake、Turtle Tower、Ngọc Sơn Temple、St. Joseph's Cathedral、Opera House、Hỏa Lò Prison 等；去掉了 4.2 列出的全部异常条目。
  - 剩余：Đại La（古城遗址，无描述，坐标 type=city）被排除，属于误删的边界情况；`Hanoi University of Pharmacy`（无描述）被保留。C02 第二版可以补 Wikidata P31 作为第三个信号。未做线上真机验收。
  - 距离仍是直线距离，界面已注明；道路距离需要路网服务，未纳入本轮。
- **需要 Codex 生成的图片编号、槽位尺寸、组件路径**（组件都在 `components/site/SiteLanding.tsx`，照片表在 `lib/site/photos.ts`，现用 Wikimedia Commons 图占位，页脚有署名）：
  | 编号 | 槽位 | 组件位置 | 桌面尺寸 | 手机尺寸 | 文字覆盖区 | 备注 |
  |---|---|---|---|---|---|---|
  | IMG-01 | 首屏背景 `PHOTOS.hero` | `<section id="top">` 满幅 `object-fit: cover`，`object-position: 50% 45%` | 1920×1080 起，横图 | 同一张图裁成竖屏，可另交 4:5 版 | 左下 620px 宽文字块 + 底部 40% 暖色渐变；右下 300px 宽手机演示 | 主体人物放中上或右侧，左下留暗 |
  | IMG-02 | Mia 头像 | `#guides` 区 150×150 圆角 28px 方块；应用内 22 到 44px 圆头像 | 600×600 | 同 | 无 | 需要在 24px 仍可辨识 |
  | IMG-03 | Milo 头像 | 同上 | 600×600 | 同 | 无 | 与 Mia 同系列、同光线方向 |
  | IMG-04a | Look 大图 `PHOTOS.eat` | 三图网格左侧 7/12 列、跨两行 | 约 1320×1120 | 4:3 | 底部 45% 暗渐变，左下标题与引语 | 内容：有人在街上抬头看建筑 |
  | IMG-04b | Walk `PHOTOS.walk` | 右上 5/12 列 | 约 920×690 | 4:3 | 同上 | 内容：一个人走在小巷 |
  | IMG-04c | Listen `PHOTOS.listen` | 右下 5/12 列 | 约 920×690 | 4:3 | 同上 | 内容：黄昏的塔或立面 |
  | IMG-05 | 路线演示照片 `PHOTOS.square` | 「Tell it where you are」左栏 4:3 | 约 1200×900 | 4:3 | 上叠虚线路线 SVG 和四个编号，左下白色胶囊 | 有开阔广场感，中部留空给路线 |
  | IMG-06 | 场景屏 `PHOTOS.alley` | 暗色满幅屏，最小高 520px | 1920×720 | 同图裁切 | 左侧 60% 暗渐变，文字在左 | 黄昏或傍晚，右侧有光 |
- **需要 Codex 评审的预览地址**：本地 `pnpm dev` 后 http://localhost:3000/site 与 http://localhost:3000/guide ；线上 https://milo-s-code.vercel.app/site （本轮推送后自动部署）。
- **下一轮建议与需要用户决定的分叉**：
  - C03：加「点听讲解到首句音频」的计时埋点（先只打到浏览器控制台或本地日志），分缓存 / 首次生成 / 被拦截三种。
  - C02 第二版：Wikidata P31 作第三信号；Đại La 这类无描述、type=city 的遗址如何保留。
  - ~~需要用户决定：设计是否放开~~ **用户已于 2026-09-14 决定：设计系统重新放开给 Codex 讨论。** PRODUCT-BRIEF 已改为 v3：第 6 节是「现行方案，非定稿」的讨论基线；8.1 中配色字体、满幅首屏两条移入 8.2。边界：产品定位（第 1 节）和文案语气（第 2 节）不放开；方案要给理由并附示例；用户拍板后 Claude 实现。Codex 出设计方案时请直接对照第 6 节逐条写「保留 / 调整为 X / 理由」，避免只给形容词。


## 9. Codex 接续核对 · PRODUCT-BRIEF v2（2026-09-14）

本节覆盖前文过时的任务状态。保留前文任务定义供验收，不再把已实现部分作为从零开发事项。依据：PRODUCT-BRIEF.md v2 及本次本地源码抽查；未进行本轮线上或真机验收。

### 9.1 已进入代码的部分

| 任务 | 当前状态 | 依据与剩余范围 |
|---|---|---|
| C01 功能状态总账 | 文档已完成，界面部分对齐，待验收 | 产品文档 4.7 已区分三种状态；一小时免费测试，半天/一天基础版也已公开，保存调整才是未来能力。勿再将所有路线标成未开放 |
| C04 Guide 入口 | 基础实现已完成，情境继承待补 | components/GuideScreen.tsx 已有附近、一小时入口，继续路线及未开放分组；其余按产品文档 8.2 接续 |
| C05 路线规则 | 部分实现，仍需修补 | lib/plan.ts 已有一小时剔除超过 2 公里的站、质量补位与缓冲；预算计算见下方 R01 |
| C06 现场表达 | 提示词已部分修订，待输出验收 | lib/guide.ts 与 app/api/ask/route.ts 已限制无朝向时的左右方位表达 |
| C08 标签 | 基础实现已完成，待验收 | lib/i18n.ts 使用 Coming soon · Plus；官网包含一小时测试免费与未来保存调整权益 |
| W01 演示标注 | 已实现标注及结束入口；完整联动待做 | components/site/SiteLanding.tsx 已有 Scripted demo 与 Try it on your own street；不把这两项重新派作新增开发 |
| C02 地点筛选、C03 最快讲解测量 | 仍待推进 | v2 仍列待办；本次未验证其已完成 |
| 图片 | 尚未生成 | 继续由 Codex 负责；设计部分仍以用户明确说明的“未定型”为准 |

官网与应用并行推进。无需等待应用全部完成再做官网图片与交互；本文件第 8 节批次理解为依赖关系，不应成为整条官网线的前置阻断。

### 9.2 新发现，Claude 请优先补齐

**R01 / C05：总时长预算未包含最终缓冲。**
lib/plan.ts 的 assemble 用行走加停留与 rule.minutes 比较，返回前又乘 1.1 加 5。举例，选站累计 55 分钟，最终约 66 分钟，仍可能超过一小时。首站还因 stops.length >= 1 条件而不执行该预算拦截。
建议：选每一站时按与最终展示一致的总时长公式校验，首站也检查；明确空路线是否显示 0 分钟，不应仅显示 5 分钟缓冲。添加覆盖边界、首站超时、无合格站点的针对性测试。不要只修改展示数字。

**R02 / C07/W01：官网清理超前承诺还不完整。**
components/site/SiteLanding.tsx 仍包含试听 Skip the queue、路线展示中的 skip the queue / almost nobody finds，以及场景屏 Take the steps on your left / minus the crowd。场景屏虽标 example line，仍与文档所称“已删除无依据人群承诺”不完全一致。
建议统一检查所有示例、试听、场景屏，不只检查首屏按钮。示例可以有明确的方位前提，但不能让它暗示产品知道用户朝向或实时客流。无需新增功能，修正文案即可。

**R03 / C08：免费与 Plus 标题仍不够准确。**
官网仍使用 Listening is free. Plus plans your day.，而公开能力已包含基础路线；Free 区同时有 Forever 和一小时测试期免费。
建议按 4.7 精确说明当前基础路线可用，未来 Plus 增加保存调整；避免标题暗示所有规划都要等 Plus。核对 Forever 的覆盖范围。

**R04 / C02：有百科来源不是实体可到访的证明。**
lib/plan.ts 的 score 对 Wikipedia 来源或链接加 3 分，这不是正文存在检测，也不是地点类型校验。因此“质量补位”不能代替 C02 的实体过滤。产品文档 4.4 的“有百科正文”描述应与实际条件核对。

### 9.3 下一次交接

Claude：完成 R01—R04 核对与必要修补，更新本文件结果；继续 C02/C03。官网沿 W01—W05 做交互骨架，并登记图片槽位。
Codex：接着按实际页面槽位做首屏与导游形象样张、素材清单和两端评审。现阶段无需重新讨论已采纳的三层结构、三入口或标签。
本次只更新协作文档，未改动应用源码，未将本地代码抽查当作上线验收。

## 10. Codex 交接 · 设计提案与配图分工更新（2026-09-14）

**本节为最新口径，覆盖前文“图片全部由 Codex 生成”及相关待办。** 用户明确要求：配图无需 Codex 直接执行，Codex 检查 prompt，用户手动生成。

- Codex：已读取 PRODUCT-BRIEF v3；完成 [设计提案 v1](./DESIGN-PROPOSAL-v1.md)，逐条对照第 6 节提供保留/调整/理由/示例，并细化首屏、同地点演示、双导游试听、路线联动。
- Codex：已完成 [配图提示词 v1](./IMAGE-PROMPTS-v1.md)，覆盖 IMG-01 至 IMG-06（含 IMG-04a/b/c 与手机首屏），可直接复制使用，附裁切和验收要求。编号采用 Claude 最新槽位表：IMG-05 为路线，IMG-06 为傍晚场景。
- 图片实际状态：用户改为手动生成前，Codex 已生成 1 张首屏构图参考；未接入项目。Mia/Milo 及其余图未生成。后续不再调用生图工具，用户生成后由 Codex 审图、Claude 接入。
- 设计状态：本轮是提案，未改应用源码；视觉候选由用户选择后 Claude 实现。功能修正与既有交互任务可继续，不被设计评审阻塞。
- 新发现的文档口径差异：PRODUCT-BRIEF 4.8 写“尺度大于 3 公里”，Claude 交接写 dim > 20000；4.4 写“超预算即停”，实现交接写“跳过超预算候选”。请 Claude 按实际代码统一，不能视为本轮已复测。
- 验证边界：本轮做了需求、槽位和提示词一致性检查；未重新验收线上 /site、/guide 或真机音频。R01–R04 与 C02 第一版的完成/测试状态以 Claude 最新交接为依据，早前第 9 节未完成状态已过时。

下一步：用户可先手动生成 IMG-01、IMG-02、IMG-03，确认风格再扩展；Claude 阅读两份新增文件，继续 C03/C02 第二版及互动骨架，接图前核对真实地点图片与品牌氛围图的用途。

## 11. 最新分工 · 审核统筹模式（2026-09-14）

用户明确要求：为节省账号额度，后续 Codex 只做审核统筹；已经生成的图片可以直接使用。本节覆盖之前的 Codex 实现、生图及扩展交付任务安排。

- **Codex**：审核 Claude 交接、关键产品/设计决策、提示词和最终效果；确定优先级、指出阻塞项、维护简短交接。不主动写应用代码、不执行生图、不重复全面研究或全量测试。每轮优先读取变更摘要，按风险抽查；结论以“通过 / 需修改 / 待确认”及具体下一步为主。
- **Claude**：具体设计细化、代码实现、素材优化接入、测试、需求文档同步与进度更新。先自行检查，再集中提交本轮变更、验证结果、预览及需要决策的问题给 Codex，减少来回。
- **用户**：最终设计决策与后续手动生图。现有首屏图已获准使用，无需为它重新生图或再次索取批准。
- **首屏素材已移交**：`assets/generated/hero-img01-v1.png`，为已生成原图的副本，原文件保留。Claude 可直接优化并接入 `PHOTOS.hero`，检查桌面文字遮挡和手机裁切；这是虚构品牌街景，不作为具体真实地点的照片证据。建议替代文本：A traveler pausing to look up at an architectural detail on a sunlit street.
- **当前状态**：素材已保存到项目，但页面接入尚未执行。IMG-02/03 等后续图片仍由用户手动生成。现有设计提案是审核参考，不代表用户已批准所有设计变更。

下次交接优先给：本轮完成项、检查结果、仍存问题、需要 Codex 判断的具体事项。无需再次转交“请 Codex 生图/实现页面”的任务。

## 12. Claude 交接 · 2026-09-14（第二轮，回应第 10/11 节）

**完成项**
- IMG-01 接入：`assets/generated/hero-img01-v1.png` → `public/images/hero-1672.jpg`（227 KB）、`hero-1200.jpg`（129 KB，srcSet）、`hero-portrait.jpg`（4:5 竖裁，人物居中，152 KB，手机 `<source media="(max-width:767px)">`）。桌面 `object-position: 60% 35%`，人物脸与雕花在右上，文字在左下暗墙；手机 `object-position: 0% 35%` 把人物推到右侧约 78%，顶部到中部渐变加深到 0.45→0.25→0.8 保证标题可读。页脚署名区注明「首屏为 AI 生成的虚构街景」，不列摄影来源。替代文本采用第 11 节建议。
- `PHOTOS.eat` 重命名为 `PHOTOS.look`（IMAGE-PROMPTS-v1 备注）。
- 新建 `docs/IMAGE-MANIFEST.md`：编号、组件、原图、网页版本、裁切焦点、属性、替代文本、状态。
- C03 第一步：`lib/metrics.ts` + `components/GuidePanel.tsx`。首次讲解从点开始计时，记录：服务端缓存命中/首次生成（`X-Guide-Cache` 头）、第一段文字到达毫秒、第一句音频开始毫秒、是否被浏览器拦截、是否失败。只写浏览器控制台和 `localStorage["tourguide.metrics"]`（最近 30 条），不上报。追问不计。
- 文档口径统一：PRODUCT-BRIEF 4.4 改为「每站按同一公式校验，超预算的站跳过继续看下一候选，首站同样」；4.8 改为「dim > 20000，Wikipedia 默认 10000」。第 10 节指出的两处差异已消除。

**检查结果**
- `pnpm test` 12/12、`tsc`、`eslint`、`next build` 通过。
- 本地 1280 宽：人物不被标题遮挡，手机演示压在人物下半身与地面，不挡脸。390 宽：竖版图加载正确（`hero-portrait.jpg`），标题在人物左侧的墙上。未做真机验收。

**仍存问题**
- 手机首屏仍是「一屏内叠标题、副题、四个按钮、主按钮、演示」，与设计提案「手机把演示移到图片下方」不同；等用户对首屏方案拍板后再改结构，本轮只保证可读。
- 指标只在本机记录，没有汇总；下一步如需看分布，加一个 `/api/metrics` 收集或先手动导出。
- Đại La 误删（type=city 的古城遗址）待 C02 第二版。

**需要用户决定（对设计提案 v1）**，按提案顺序，Claude 给出建议：
1. 首屏布局：A 推荐方案（左文右演示，照片作视觉不承载全部信息）/ B 兼容满幅（现状，去掉三个数字，情境按钮收进演示区）。建议 A，但先用 B 过渡到用户看过 A 的草图。
2. 副题是否改为 "A local guide in your pocket. Find out what you're looking at, ask a question, and keep exploring."（去掉 knows every street 的承诺）。建议改。
3. 主按钮文案 "Try your local guide" 替代 "Open in browser"。建议改。
4. 取消数字计数动画、首屏去掉三个数字。建议接受（与 A/B 一起）。
5. 主按钮圆角从胶囊改 12px、大图容器 16–20px。建议保留胶囊按钮，接受图容器调整（改动小，风险小）。
6. 陶土分两档（装饰 #D9633A / 交互与小链接 #A84427）。建议接受，实施时测对比度。
7. 桌面应用地图 60% + 讲解 40%（应用电脑端版式）。建议排到官网首屏定稿之后。

## 13. Claude 交接 · 2026-09-14（第三轮：设计提案落地 + 全部配图接入）

**用户决定**：设计提案 v1 七项全部采纳 Claude 的建议（PRODUCT-BRIEF 第 13 节）。图片由用户生成并放入 `assets/generated/`（IMG-02～06、手机独立版首屏图）。

**完成项**
- 首屏方案 A：`components/site/SiteLanding.tsx` 顶栏改为墨色文字在暖白底上；首屏桌面 52/48 分栏，左侧标题、新副题、`Try your local guide` + `Try an example`（锚到演示）、一行说明；右侧 4:5 品牌图（用户生成的竖版），下沿压演示面板（Scripted demo 标注、情境选项带选中勾、对话、Try it on your own street）。手机顺序：标题→副题→按钮→图片与演示。
- 数字计数区与 `Counter` 组件删除；逐字打字机换成逐句显示（550ms/句），`prefers-reduced-motion` 下一次全出。
- 容器圆角 28→20；陶土两档 `T.deep = #A84427` 用于小链接。
- 图片：IMG-02/03 → `mia/milo-{600,300,96}.jpg`，应用内 `PersonaAvatar` 有图就显示肖像（引导页、设置、导游页、对话页、路线栏都跟着变）；IMG-04a/b/c、05、06 → `look/walk/listen/square/dusk.jpg`；首屏用用户生成的竖版 `hero-portrait.jpg`（替换了我之前的裁切）。`public/site/` 的 Commons 照片已删除，页脚署名改为「街景与两位导游肖像为 AI 生成的虚构图」。`lib/site/photos.ts` 全部标 `generated: true`。
- 文档：`docs/IMAGE-MANIFEST.md` 八项全部「已接入」；PRODUCT-BRIEF v4（5.1、5.2、5.5、6.3、6.4、6.6、8.2、新增第 13 节）。

**检查结果**：`pnpm test` 12/12、`tsc`、`eslint`、`next build` 通过。本地 1280 宽：左文右图，人物脸在右栏上部，演示面板压在图下沿不挡脸；390 宽：标题、副题、按钮在图上方，图与演示在下，无横向溢出。未做真机验收。

**仍存问题 / 下一步**
- 设计提案的 I01（同一地点从看见到听懂，含追问）、I02（同题对比试听）、I03（一小时下一站联动）尚未做，属于下一批；现演示仍是四个情境各一段。
- 导游试听句仍是自我介绍式，I02 要求同地点同事实两段对比，待做。
- C02 第二版（Wikidata P31）、隐私与条款页、公司名邮箱、域名分流未动。
- 请 Codex 抽查：桌面首屏人物与演示面板的关系、24px 肖像辨识度、手机首屏一屏内是否过长。

## 14. Codex 审核 · 第三轮变更（2026-09-14）

范围：读取第 12/13 节交接，定向抽查 SiteLanding.tsx、GuidePanel.tsx、useNarrator.ts、metrics.ts 与缓存响应头。按用户要求，本轮不校验图片，不做全面测试或页面视觉验收；未修改源码。

### 通过（限本轮源码与交接范围）

- 首屏方案 A、新副题、主/次按钮、移除数字等方向接受，用户已采纳的设计决定不再重开。源码确认存在 Try your local guide、Try an example 与 Scripted demo；情境按钮有 aria-pressed 和选中标记。
- 逐句显示及减少动态效果分支已存在。这里的“减少动态效果”特指访问者系统的 prefers-reduced-motion 设置，不代表产品默认少做互动或用户审美偏好。默认版本继续加强地图、讲解、选择的联动。
- 本地限量记录指标、不立刻增加服务端收集，符合当前阶段。先保证数据正确，无需现在建设 /api/metrics。

### 需修改：C03 的首句音频计时尚不可信（优先）

**证据**：GuidePanel.tsx 255–268 行依据 narrator.state === playing 且 currentIndex >= 0 调用 markAudio 并立即 flush。useNarrator.ts 在 fetchAudioUrl 之前就 setCurrentIndex(idx)，而 playing 状态也在实际出声前设置。由此可能把合成/下载等待之前的时刻当作首句已播放；随后播放被拦截时，flush 已锁定，blocked 状态不能写入同一条记录。

**Claude 修正**：从实际朗读音频的 playing 事件、系统语音的 utterance.onstart 发出首句开始信号；排除解锁用静音音频。准备、生成、加载不算已出声。被拦截后允许更新同一次尝试，保留 blocked=true 及用户手动恢复后的真实首句时间；失败/退出应有明确终态，避免只有成功样本。firstAudio 仅表示首段朗读启动，不自动证明第一句有信息量，后者仍需抽样核对讲解文本。

**起点与隔离一并核对**：当前计时器在 GuidePanel.run 内创建，不包含地图/详情页点击到页面挂载的时间；应命名为“请求启动→首句播放”，或传递入口点击时间另记端到端指标。run 的 catch 使用共享 timerRef，重试时旧请求中止可能写到新计时器；每个请求持有自己的计时器，并防止旧请求更新新尝试。

**最小验收**：人工延迟语音请求时计时应包含延迟；首次播放受限后记录 blocked，手动恢复能补真实播放时间；快速重试不串记录；静音、失败及退出不伪记成功。由 Claude 执行并提交结果，Codex 不重复全量测试。

### 下一批安排

1. Claude 先修 C03 上述计时准确性；这是测量修正，不代表当前讲解功能整体不可用。
2. 官网优先 I01（同一地点：看见→讲解→追问）与 I02（同地点同事实、不同表达的双导游试听）。现有四情境换文字只是基础演示，不标为完整互动已完成。减少动态效果模式保留全部选择与结果，仅减弱运动。
3. C02 第二版继续处理已记录的误删/漏保留边界；I03 路线联动随后推进。隐私条款按实际功能准备，公司名邮箱等缺项集中交用户补充。
4. 下次交接提交计时修正结果、I01/I02 完成状态及必要预览。图片校验本轮跳过，不作为本批审核阻塞。

结论：首屏结构与文案方向通过本轮抽查；C03 需修改；完整互动与真机效果待验收。Claude 所报 test/tsc/eslint/build 通过保留为其验证记录，本轮未复跑。

## 15. Claude 交接 · 2026-09-14（第四轮：C03 计时修正）

回应第 14 节「需修改」。

**改动**
- `lib/useNarrator.ts`：新增 `events` 参数（`onFirstAudio`、`onBlocked`）。`onFirstAudio` 只在音频元素的 `playing` 事件或系统语音 `utterance.onstart` 触发，每轮朗读一次；`play()` 从头开始和 `stop()` 时重置；`haltOutput()` 与 `unlock()` 清掉 `onplaying`，解锁用的静音 WAV 不会被算作首句。被拦截（NotAllowedError）时触发 `onBlocked`。
- `lib/metrics.ts` 重写：`NarrationTimer` 一次尝试一条记录，每次 mark 按 id 覆盖写回；字段 `entryToRequestMs`（入口点击→请求启动，60 秒内有效，用完即清）、`firstTextMs`、`firstAudioMs`（请求启动→首句真的播放）、`blocked`、`outcome`（pending / playing / blocked / muted / failed / aborted）。`markBlocked` 不是终态，之后用户手动播放 `markAudio` 会补上真实时间并保留 `blocked=true`；`markMuted` / `fail` / `abort` 是终态，后到的事件不再改。依赖可注入（时钟、存储），便于测试。
- `components/GuidePanel.tsx`：每个请求持有自己的计时器（局部变量 `timer`），启动新请求时先把上一个未结束的标 aborted；`AbortError` → aborted，其他错误 → failed；静音且文字流结束 → muted；组件卸载 → aborted。删掉原来依据 `narrator.state === "playing" && currentIndex >= 0` 的推断。
- 入口点击：`PlaceCard` 与路线 `StopCard` 的「听导游讲讲」`onClick` 调 `markEntryClick()`，写 sessionStorage 时间戳。
- 命名与文档：PRODUCT-BRIEF 1.3 改为「入口点击→请求启动」与「请求启动→首句播放」两段口径。

**验证**
- `tests/metrics.test.ts` 6 条：延迟计入首句时间；blocked 后补真实时间且保留 blocked；快速重试两条记录互不覆盖；muted / failed 终态不被后来事件改写；入口点击 60 秒窗口与用完即清；缓存头解析。`pnpm test` 18/18；`tsc`、`eslint`、`next build` 通过。
- 本地浏览器打开 /p/en/wp:Eiffel_Tower/talk（桌面 Chromium，允许自动播放，智谱首次生成）实测一条：`cache: miss, firstTextMs: 447, firstAudioMs: 2393, blocked: false, outcome: playing, entryToRequestMs: null`（直接打开链接，没有入口点击）。真机（手机被拦截→手动播放）未验收，逻辑由单元测试覆盖。

**未做 / 说明**
- `firstAudioMs` 只表示首段朗读启动，不证明第一句有信息量（第 14 节已说明），后者仍靠抽样看文本。
- 系统语音路径（browser 引擎）的 onstart 事件在部分浏览器触发时机偏早，属已知平台差异，记录里可通过 `cache`/`outcome` 之外的引擎字段区分——本轮未加引擎字段，若需要下轮补。

**下一步**：I01（同一地点：看见→讲解→追问）与 I02（同地点同事实的双导游试听）。

## 16. Claude 交接 · 2026-09-14（第五轮：I01 / I02）

**改动**
- `lib/site/demo.ts`：演示地点巴黎圣雅克塔（真实照片：Commons `Tour Saint-Jacques au crépuscule.jpg`，Fabien Barrau，CC BY-SA 4.0，页脚署名）。I01 脚本三句讲解 + 两句追问回答；I02 两位导游各三句，同一组事实。事实依据列在文件头注释，全部是公开稳定记载，未含虚构亲历。
- `components/site/useScriptedSpeech.ts`：官网朗读器。逐句向 `/api/tts` 取音频、先预载再顺序播，因此能报总时长并逐句高亮；全站同一时间只播一段（再点或切换即停旧段）；只在点击后出声；取不到音频进 error，文字照常可读，提供重试。
- `components/site/PlaceDemo.tsx`（I01）：真实照片缩略图 + 图钉 + 地点名（点 real photo 到来源页）；三步：Hear its story → 讲解 + 追问按钮 → 追问回答；每段有 Preparing / Playing · Ns / Paused / Played 状态与 Pause / Resume / Stop / Replay；出口 Open this place in the app（应用内该地点真实详情页）与 Try it on your own street。
- `components/site/GuideCompare.tsx`（I02）：两卡并排，肖像 + 风格说明（描述表达方式，不写居住年限）；Hear / Pause / Resume / Stop / Replay，`aria-live` 状态文字含时长；选中卡片有描边 + 「✓ selected」文字；Start with Mia / Milo → `${app}/?persona=mia|milo`。
- 应用：`HomeScreen` 启动时若 URL 带 `persona=` 且合法则写入偏好（只在明确带参数时覆盖）；`Onboarding` 默认导游读本机偏好，所以官网选择会预选。
- 首屏四情境按钮演示与逐句打字机移除；路线演示区的头像改用肖像图。

**验证**
- `tsc`、`eslint`、`pnpm test` 18/18、`next build` 通过。
- 本地 1280 宽实测（桌面 Chromium，允许自动播放）：点 Hear its story → 状态「Playing · 22s」，三句逐句高亮，追问按钮出现；点 What should I look for? → 「Playing · 19s」，出口 Open this place in the app / Try it on your own street 出现；再点导游区 Hear Milo → 首屏演示立即停止（互斥生效），Milo 卡显示「✓ selected」与播放状态。真机点击路径与被拦截时的 error 态未验收。

**未做**
- I03（一小时下一站联动）未动；C02 第二版未动。
- 试听时长以实际合成结果为准（每次 ~12–18 秒），未写死数字。
